# @package _global_
defaults:
  - _default
model:
  task:
    tasks: [ModelTask.shuffle_infill, ModelTask.kinematic_decoding, ModelTask.constraints]
    encode_constraints: True
    use_constraint_cls: True
    covariate_mask_ratio: 1.0
dataset:
  sparse_constraints: True
inherit_tag: sparse_constraint_enc
train:
  batch_size: 8 # always OOM and regardless of training steps # (pretrain 4 gpus), yikes, 16 is too high
load_from_id: sparse_constraint_no_enc-67jq6ci1