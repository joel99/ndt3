# @package _global_
defaults:
  - _default
dataset:
  datasets:
  - odoherty_rtt.*
model:
  task:
    tasks:
      - ModelTask.spike_context
      - ModelTask.kinematic_classification
    covariate_mask_ratio: 1.0
train:
  batch_size: 16